<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.bubblepedia.WikiBubbleActivity">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/btnGoToTag">
        <EditText
            android:id="@+id/bubbleTextview"
            android:layout_width="match_parent"
            android:layout_height="20dp"
            android:hint="@string/tell_us_your_thoughts"
            android:inputType="textMultiLine"
            android:paddingBottom="10dp"
            android:scrollbarStyle="insideOverlay" />
    </ScrollView>

    <Button
        android:id="@+id/btnGoToTag"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:hint="Tag your post to wiki"
        android:text="Tag"
        android:onClick="gotoSearchableActivity"
        />

    <Button
        android:id="@+id/btnPost"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentLeft="true"
        android:layout_alignParentBottom="true"
        android:hint="Tag your post to wiki"
        android:text="Post Content"
        android:onClick="postContentWithoutTag"/>

</RelativeLayout>
